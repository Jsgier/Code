#include "msp.h"


/**
 * main.c
 */
void main(void)
{

    TIMER_A0->CTL = TIMER_A_CTL_IE //start timer A0, enable interrupt,


	WDT_A->CTL = WDT_A_CTL_PW | WDT_A_CTL_HOLD;		// stop watchdog timer
	while(1)                                        // infinite loop
}
